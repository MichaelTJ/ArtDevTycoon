node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report

# AI model weights. Models are fetched by the browser at runtime and cached by the
# browser, never committed. This only catches a stray local download.
models
*.onnx

.tmp

# Modifier explorer output (dev-only batch generations)
data/modifier-explorer/
data/modifier-explorer2/
